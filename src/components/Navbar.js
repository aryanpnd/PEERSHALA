import React from 'react'
import Button from '@mui/material/Button';
import { Box } from '@mui/material';

function navbar() {
  return (
    <Box component="span" sx={{ display: { xs: 'none', lg: 'block', xl: 'none' } }}>block</Box>
  )
}

export default navbar